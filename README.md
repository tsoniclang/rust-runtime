# rust-runtime

Core Rust runtime crate for Tsonic-emitted Rust.

This repository mirrors the C# runtime split: `rust-runtime` owns the shared
runtime ABI, while `rust-js` and `rust-nodejs` own JS and Node surface runtime
APIs.

## Crate

- Package/crate: `tsonic_rust_runtime`
